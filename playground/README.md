# Simple Playground

This playground is a bare minimum playground only for catching rendering errors. 
There is no support for actual communication, since there is no proxy or OpenAI secrets included. 